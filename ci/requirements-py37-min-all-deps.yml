name: xarray_mongodb
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.7
  - dask=2.24
  - numpy=1.18
  - pandas=1.0
  # - pint  # Requires NEP18; see requirements-py37-min-nep18
  - pymongo=3.11
  - pytest
  - pytest-asyncio
  - pytest-cov
  # - sparse  # Requires NEP18; see requirements-py37-min-nep18
  - toolz=0.10
  - xarray=0.16

  - pip:
    # builds temporarily missing in conda (see above)
    - motor >= 2.2