name: test_env
channels:
  - conda-forge
  - anaconda
dependencies:
  - python=3.7
  - black
  - dask
  - flake8
  # - motor  # Doesn't support Windows
  - mypy
  - numpy
  - pandas
  - pint
  - pymongo
  - pytest
  - pytest-asyncio
  - pytest-cov
  - sparse
  - toolz
  - xarray
