name: test_env
channels:
  - anaconda
  - conda-forge
dependencies:
  - python=3.7
  - dask
  - motor
  - numpy>=1.17
  - pint>=0.9
  - pymongo
  - pytest
  - pytest-asyncio
  - pytest-cov
  - sparse>=0.8
  - xarray
  # code linters (running only in the latest version of Python)
  - black
  - flake8
  - mypy
