name: test_env
channels:
  - conda-forge
dependencies:
  # python=3.7.1=h5001a0f_0 is broken
  - python=3.7.1=h0371630_7
  - coveralls
  - dask
  - motor
  - pymongo
  - pytest
  - pytest-asyncio >=0.10
  - pytest-cov
  - xarray
