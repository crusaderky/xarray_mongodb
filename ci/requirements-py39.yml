name: xarray_mongodb
channels:
  - conda-forge
  - defaults
  - numba/label/dev  # Numba dev branch, needed until numba 0.53 is released
dependencies:
  - python=3.9
  - dask
  - motor
  - numpy
  - pandas
  - pint
  - pymongo
  - pytest
  - pytest-asyncio
  - pytest-cov
  - sparse
  - toolz
  - xarray
