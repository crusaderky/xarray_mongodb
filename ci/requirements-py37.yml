name: test_env
channels:
  - anaconda
  - conda-forge
dependencies:
  - python=3.7
  - dask
  - motor
  - pymongo
  - pytest
  - pytest-asyncio
  - pytest-cov
  - xarray
  # code linters (running only in the latest version of Python)
  - black
  - flake8
  - mypy
