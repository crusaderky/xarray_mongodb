name: xarray_mongodb
channels:
  - conda-forge
dependencies:
  # Core dependencies
  - dask
  - numpy
  - pandas
  - pymongo
  - toolz
  - xarray

  # Optional dependencies
  - motor
  - pint
  - sparse

  # Test tools
  - pytest
  - pytest-asyncio
  - pytest-cov
