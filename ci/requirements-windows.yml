name: test_env
channels:
  - anaconda
  - conda-forge
dependencies:
  - python=3.7
  - dask
  - numpy>=1.17
  - pint>=0.9
  - pymongo
  - pytest
  - pytest-asyncio
  - pytest-cov
  - sparse>=0.8
  - xarray

# Note: motor 2.0 doesn't support Windows
